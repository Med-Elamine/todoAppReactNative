import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    touch: {
        borderColor: '#569',
        borderWidth: 1,
        borderStyle: 'dashed',
        borderRadius: 10,
        padding: 16,
        marginTop: 16,
    },
    item: {
        alignSelf: 'center'
    }
});

export default Styles;